version: "3"
services:
  react-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      #- .:/app
      #- /app/node_modules
      - ./src:/app/src
    environment:
      - NODE_ENV=development